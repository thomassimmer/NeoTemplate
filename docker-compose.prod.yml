services:
  db:
    container_name: db
    image: postgres:14
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "10"
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./backend/.env
    ports:
      - "5434:5434"
    command: -p 5434
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U neotemplate_db -p 5434"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    container_name: backend
    image: backend
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "10"
    command: >
      bash -c "python manage.py makemigrations
      && python manage.py migrate
      && python manage.py collectstatic --no-input
      && python manage.py shell < api/scripts/generate_languages.py
      && python -m uvicorn core.asgi:application --host 0.0.0.0 --port 8000"
    restart: unless-stopped
    build:
      context: ./backend
    hostname: backend
    env_file:
      - ./backend/.env
    # No volume mount in production - code should be in image
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'python -c "import urllib.request; urllib.request.urlopen(''http://localhost:8000/api/'')" || exit 1',
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    container_name: frontend
    image: frontend
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "10"
    restart: unless-stopped
    build:
      context: ./frontend
      target: runner-prod
    env_file:
      - ./frontend/.env
    # No volume mount in production - code should be in image
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  postgres_data:
